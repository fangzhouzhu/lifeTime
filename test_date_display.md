# 日期显示修复说明

## 修复的问题

### 1. 未来日期显示错误

**问题**：选择明天的日期，显示"-1 年"
**原因**：未正确判断未来日期，导致年份计算为负数
**修复**：在 `Anniversary.yearsPassed` 中添加未来日期判断逻辑

### 2. 时间显示不够智能

**问题**：所有时间差都显示为"年"
**原因**：`formatYearsPassed` 方法只考虑年月日，没有根据实际时间差智能显示
**修复**：新增 `formatTimeDifference` 方法，根据时间长短智能显示

## 修复后的显示逻辑

### 时间差智能显示规则：

1. **小于 1 分钟**: 显示秒（如"30 秒"）
2. **小于 1 小时**: 显示分钟+秒（如"5 分钟 30 秒"）
3. **小于 1 天**: 显示小时+分钟（如"3 小时 20 分钟"）
4. **小于 30 天**: 显示天+小时（如"15 天 6 小时"）
5. **小于 365 天**: 显示月+天（如"3 个月 15 天"）
6. **大于 365 天**: 显示年+月或年+天（如"2 年 3 个月"、"1 年 100 天"）

### 示例对比

#### 过去日期

- **5 秒前**: `5秒`
- **3 分钟前**: `3分钟`
- **2 小时前**: `2小时`
- **3 天前**: `3天`
- **2 个月前**: `2个月`
- **1 年前**: `1年`
- **2 年 3 个月前**: `2年3个月`

#### 未来日期

- **明天**:
  - 已过/还有：`还有 1天`
  - 距今：`还有1天`
- **下周**:
  - 已过/还有：`还有 7天`
  - 距今：`还有7天`
- **1 个月后**:
  - 已过/还有：`还有 1个月`
  - 距今：`还有30天`

#### 今天

- **今天设置的纪念日**:
  - 已过/还有：`0秒`（刚刚）
  - 下一个纪念日：`就是今天！`

## 关键代码改动

### 1. DateFormatter.formatTimeDifference()

新增方法，根据时间长短智能返回格式化字符串：

```dart
static String formatTimeDifference(Duration duration, {bool isFuture = false}) {
  // 根据duration的长短返回最合适的显示格式
  // 优先级：秒 -> 分钟 -> 小时 -> 天 -> 月 -> 年
}
```

### 2. Anniversary.yearsPassed

修复未来日期计算 bug：

```dart
int get yearsPassed {
  // 如果是未来日期，返回负数
  if (targetDate.isAfter(today)) {
    int years = date.year - now.year;
    // 正确计算未来年份
    return -years;
  }
  // 过去日期正常计算
  return years;
}
```

### 3. Anniversary.nextAnniversary

修复今天的判断逻辑：

```dart
DateTime get nextAnniversary {
  // 正确判断是否是今天
  if (nextDay.isAtSameMomentAs(today)) {
    return next; // 今天就返回今天
  }
  // 其他逻辑...
}
```

### 4. 详情页面显示

根据日期是过去还是未来动态调整标签：

```dart
_buildInfoItem(
  isFuture ? '还有' : '已过',  // 动态标签
  DateFormatter.formatYearsPassed(widget.anniversary.date),
  color,
),
```

## 测试场景

建议测试以下场景以验证修复：

1. ✅ 创建今天的纪念日
2. ✅ 创建昨天的纪念日
3. ✅ 创建明天的纪念日
4. ✅ 创建一周前的纪念日
5. ✅ 创建一个月前的纪念日
6. ✅ 创建一年前的纪念日
7. ✅ 创建多年前的纪念日
8. ✅ 创建未来几天的纪念日
9. ✅ 创建未来几个月的纪念日
10. ✅ 创建未来几年的纪念日

所有场景现在都应该显示正确的时间差和合适的时间单位！
